# Required — OpenAI Whisper transcription
OPENAI_API_KEY=

# Required for transcription — WhatsApp group JID (ends with @g.us)
# Leave empty on first run: the app lists your groups after QR scan.
# Then set it and restart, e.g.:
# TRANSCRIBE_GROUP_JID=1203xxxxxxxxxx@g.us
TRANSCRIBE_GROUP_JID=
